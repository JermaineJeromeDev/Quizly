"""Defines the data models for the authentication app, including user profiles, authentication tokens, and any related entities necessary for managing user accounts and authentication processes. Currently, no custom models are defined, but this file serves as a placeholder for future enhancements to the data schema of the authentication system."""

from django.db import models

# Create your models here.
