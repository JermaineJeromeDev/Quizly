""" "Defines the admin interface for the authentication app, allowing administrators to manage user accounts and related models through Django's built-in admin site.
Currently, no custom admin models are registered, but this file serves as a placeholder for future enhancements to the admin interface.
"""

from django.contrib import admin

# Register your models here.
